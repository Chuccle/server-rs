[package]
name = "server-rs"
version = "0.1.0"
edition = "2021"

[features]
default = [ "stats" ]
logging = ["dep:log", "dep:env_logger", "dep:dotenv"]
stats = ["logging"]

[dependencies]
actix-files = "0.6.6"
actix-web = "4.9.0"
dotenv = { version = "0.15.0", optional = true }
env_logger = { version = "0.11.6", optional = true }
flatbuffers = "25.2.10"
log = { version = "0.4.25", optional = true }
scc = "2.3.0"
serde = { version = "1.0.217", features = ["derive"] }
thiserror = "2.0.11"
tokio = { version = "1.43.0", features = ["fs"] }

[dev-dependencies]
tempfile = "3.3.0"
urlencoding = "2.1.2"
actix-rt = "2.0"
tokio = { version = "1.0", features = ["rt", "macros", "test-util"] }
